{
  "name": "tx",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "tx": "./src/cli.ts"
  },
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "typecheck": "tsc",
    "test": "bun test",
    "build": "bun build --compile --minify --outfile dist/tx src/cli.ts",
    "check": "bun run lint && bun run typecheck && bun run test && bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
